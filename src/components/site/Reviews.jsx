import React from "react";

export default function Reviews() {
  // TODO: replace with real review data once backend (Supabase/etc.) is set up
  const reviews = [];
  if (!reviews.length) return null;

  return null; // placeholder — component returns nothing until real data exists
}